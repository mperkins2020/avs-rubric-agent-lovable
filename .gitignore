# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local

# Backups (contain PII — never commit). Keep the .example template tracked.
tools/backup/dumps/
tools/backup/.env.backup
!tools/backup/.env.backup.example

# Pre-rescan snapshots (snapshot-scan.ts) — local working data, recoverable
# from scan_results directly, not meant to be version-controlled.
tools/scraper-dev/snapshots/
